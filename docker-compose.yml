version: '3.8'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=${SECRET_KEY:-dev-secret}
    volumes:
      - ./:/app:ro
      - ./static:/app/static
      - ./bounty.db:/app/bounty.db
    restart: unless-stopped
